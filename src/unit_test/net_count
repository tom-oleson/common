echo "final totals = "$(grep 1000000 log/network_test.log | wc -l)
echo "requests     = "$(grep request log/network_test.log | wc -l)
echo "responses    = "$(grep response log/network_test.log | wc -l)

